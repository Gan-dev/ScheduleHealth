<div class="navBars">
    <nav class="nav flex-column">
        <a class="nav-link active" aria-current="page" href="/news">news</a>
        {{#each currentUser.newsPreferences}}
        <a class="nav-link active" aria-current="page" href="/news/{{this}}">{{this}}</a>
        {{/each}}

        <a class="nav-link" href="/myprofile">Return</a>

        {{#if currentUser}}
        <form action="/logout" method="POST">
            <input type="submit" class="nav-link" value="Logout"
                style="background-color: white; border: none; color: rgb(221, 74, 74)">
        </form>
        {{/if}}
    </nav>
</div>